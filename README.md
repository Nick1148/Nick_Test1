# 🏭 4GTP 다중 타겟 예측 시스템

공장 공정 데이터를 활용한 다중 타겟 예측 모델 시스템입니다.

## 📌 프로젝트 개요

BTX 생산 공정에서 **3개의 핵심 타겟 변수**를 동시에 예측하여 운영 효율을 분석합니다.

### 타겟 변수
| 변수 | 설명 | 목적 |
|------|------|------|
| BTX_generation | BTX 생산량 | 생산성 최적화 |
| Heater_steam_input | 히터 증기 투입량 | 에너지 효율 |
| BTXdistillator_distl_steam_input | BTX 증류탑 증기 투입량 | 에너지 효율 |

### 모델 구성
- **통합 모델**: 전체 데이터 학습
- **ClassA 모델**: ClassA 석탄 전용
- **ClassB 모델**: ClassB 석탄 전용
- **ClassC 모델**: ClassC 석탄 전용

---

## 🚀 빠른 시작

### 1. 환경 설정

```bash
# 가상환경 생성 (선택사항)
python -m venv venv
venv\Scripts\activate  # Windows

# 의존성 설치
pip install -r requirements.txt
```

### 2. 모델 학습

```bash
# 방법 1: Python 스크립트
python run_training.py

# 방법 2: 배치 파일 (Windows)
run_training.bat
```

### 3. 웹 앱 실행

```bash
# 방법 1: 명령어
streamlit run app/streamlit_app.py

# 방법 2: 배치 파일 (Windows)
run_app.bat
```

브라우저에서 `http://localhost:8501` 접속

---

## 📁 프로젝트 구조

```
화성공장/
├── 📄 4GTP_integrated_with_coal_Raw.xlsx  # 원본 데이터
├── 📄 SPECKIT_다중타겟예측모델.md          # 프로젝트 명세서
├── 📄 requirements.txt                    # 의존성 목록
├── 📄 run_training.py                     # 학습 실행 스크립트
├── 📄 run_training.bat                    # 학습 실행 (Windows)
├── 📄 run_app.bat                         # 앱 실행 (Windows)
├── 📄 README.md                           # 이 파일
│
├── 📂 config/                             # 설정 파일
│   ├── __init__.py
│   └── settings.py                        # 전역 설정
│
├── 📂 src/                                # 소스 코드
│   ├── __init__.py
│   ├── preprocessing.py                   # Module 1: 전처리
│   ├── training.py                        # Module 2: 모델 학습
│   └── prediction.py                      # 예측 모듈
│
├── 📂 app/                                # Streamlit 앱
│   └── streamlit_app.py                   # Module 3: 시각화/UI
│
├── 📂 models/                             # 학습된 모델 저장
│   ├── model_integrated.pkl
│   ├── model_ClassA.pkl
│   ├── model_ClassB.pkl
│   ├── model_ClassC.pkl
│   ├── preprocessor.pkl
│   └── metadata.json
│
├── 📂 notebooks/                          # Jupyter 노트북
│
└── 📂 assets/                             # 정적 자원
```

---

## 🖥️ UI 기능

### 1. 대시보드 (🏠)
- 시스템 개요 및 주요 지표
- Coal Class 분포 차트
- 타겟 변수 최근 추이

### 2. 데이터 분석 (📊)
- 데이터 기본 정보
- 기술 통계량
- 상관관계 분석
- 분포 분석

### 3. 모델 학습 (🤖)
- AutoML/sklearn 학습 실행
- 성능 게이지 차트
- 학습 설정 관리

### 4. 예측 실행 (🔮)
- 수동 입력 예측
- 데이터 기반 배치 예측
- 예측값 vs 실제값 비교
- 효율 분석

### 5. 결과 분석 (📈)
- Feature Importance 시각화
- 예측 기록 조회
- CSV 다운로드

---

## 📊 성능 지표

| 모델 | BTX 생산량 (R²) | 히터 증기 (R²) | BTX증류 증기 (R²) |
|------|----------------|----------------|-------------------|
| 통합 | 0.85+ | 0.75+ | 0.75+ |
| ClassA | 0.80+ | 0.70+ | 0.70+ |
| ClassB | 0.85+ | 0.75+ | 0.75+ |
| ClassC | 0.75+ | 0.65+ | 0.65+ |

---

## 🔧 주요 기술 스택

- **Python 3.9+**
- **Streamlit**: 웹 UI
- **PyCaret / scikit-learn**: AutoML / ML
- **XGBoost / LightGBM**: 고성능 앙상블
- **Plotly**: 인터랙티브 시각화
- **Pandas / NumPy**: 데이터 처리

---

## 📝 사용 예시

### Python에서 직접 예측

```python
from src.prediction import MultiTargetPredictor
import pandas as pd

# 예측기 로드
predictor = MultiTargetPredictor()
predictor.load_models()

# 입력 데이터 준비
input_data = {
    'BS_out_COG_T': 26.5,
    'BS_out_COG_P': 645.0,
    'BS_out_COG_F': 85.0,
    # ... 기타 피처
}

# 예측 실행
X = pd.DataFrame([input_data])
predictions = predictor.predict_all_models(X)

print(predictions)
```

---

## ⚠️ 주의사항

1. **데이터 파일**: `4GTP_integrated_with_coal_Raw.xlsx`가 루트 디렉토리에 있어야 합니다.
2. **모델 학습**: 처음 사용 시 `run_training.py`를 먼저 실행하세요.
3. **메모리**: 전체 데이터 학습 시 8GB+ RAM 권장

---

## 📞 지원

문의사항이 있으시면 담당자에게 연락하세요.

---

© 2025 4GTP 다중 타겟 예측 시스템 | Powered by Claude
