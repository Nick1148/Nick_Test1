# 다중 타겟 예측 모델 프로젝트 명세서 (SpecKit)

> **프로젝트명**: 4GTP 다중 타겟 예측 모델
> **버전**: 1.0
> **작성일**: 2025-11-28
> **데이터 소스**: 4GTP_integrated_with_coal_Raw.xlsx

---

## 목차

1. [프로젝트 개요](#1-프로젝트-개요)
2. [데이터 명세](#2-데이터-명세)
3. [개발 모듈 구조](#3-개발-모듈-구조)
   - [Module 1: 데이터 전처리](#module-1-데이터-전처리-preprocessing)
   - [Module 2: 모델 학습](#module-2-모델-학습-training)
   - [Module 3: 시각화 및 UI](#module-3-시각화-및-ui-visualization)
4. [기능 요구사항](#4-기능-요구사항)
5. [성능 평가 지표](#5-성능-평가-지표)
6. [배포 및 운영](#6-배포-및-운영)
7. [향후 계획](#7-향후-계획)

---

## 1. 프로젝트 개요

### 1.1 목적

공장의 공정 데이터를 활용하여 **다중 타겟 예측 모델**을 개발합니다. 하나의 모델로 BTX 생산량, Heater 증기 투입량, BTX 증류기 증기 투입량 등 여러 출력 변수를 **동시에 예측**하여, 각각 개별 모델을 운용할 때보다 출력 간 일관성 있게 값을 산출하는 것이 목표입니다.

### 1.2 기대 효과

- 출력 변수들 사이의 상호 연관성을 고려한 일관된 예측
- 예측 결과와 현재 운영 상황 비교를 통한 효율 정량 평가
- 데이터 기반 공정 최적화 인사이트 도출

### 1.3 타겟 변수 (Target Outputs)

| 변수명 | 설명 | 단위 (추정) |
|--------|------|-------------|
| `BTX_generation` | BTX(Benzene, Toluene, Xylene) 총 생산량 | ton/hr |
| `Heater_steam_input` | 히터에 투입되는 증기 유량 | ton/hr |
| `BTXdistillator_distl_steam_input` | BTX 증류탑에 투입되는 증기 유량 | ton/hr |

### 1.4 주요 사용 사례 (User Stories)

1. **엔지니어**: 현재 공정 운영 데이터 입력 → 모델 예측 → 현재 값과 비교하여 운영 효율 개선 가능성 확인
2. **현장 운영자**: 간편한 UI에서 운전 조건 입력 → 즉시 예측 결과 확인 → 실시간 대응

---

## 2. 데이터 명세

### 2.1 데이터셋 기본 정보

| 항목 | 값 |
|------|-----|
| 파일명 | `4GTP_integrated_with_coal_Raw.xlsx` |
| 행 수 | 37,039 |
| 열 수 | 32 |
| 기간 | 2021-02-01 ~ 2025-06-17 |
| 샘플링 주기 | 1시간 (Hourly) |
| 결측치 | 없음 |

### 2.2 입력 변수 (Features)

#### 가스/오일 관련 변수
| 변수명 | 설명 | 데이터 타입 |
|--------|------|-------------|
| `BS_out_COG_T` | BS 출구 COG 온도 | float64 |
| `BS_out_COG_P` | BS 출구 COG 압력 | float64 |
| `BS_out_COG_F` | BS 출구 COG 유량 | float64 |
| `BS_aoil_flow` | 흡수유 유량 | float64 |

#### 온도/압력 관련 변수
| 변수명 | 설명 | 데이터 타입 |
|--------|------|-------------|
| `Tank_RO_T` | 탱크 리치오일 온도 | float64 |
| `HE_VO_RO_T` | 열교환기 VO 리치오일 온도 | float64 |
| `HE_OO_RO_T` | 열교환기 OO 리치오일 온도 | float64 |
| `HE_OO_RO_P` | 열교환기 OO 리치오일 압력 | float64 |
| `TOP_out_AO_T` | TOP 출구 흡수유 온도 | float64 |
| `HE_OO_AO_T` | 열교환기 OO 흡수유 온도 | float64 |
| `HE_OW_AO_T` | 열교환기 OW 흡수유 온도 | float64 |
| `HE_spiral_AO_T` | 열교환기 spiral 흡수유 온도 | float64 |
| `HE_VO_V_T` | 열교환기 VO 증기 온도 | float64 |
| `HE_VW_W_T` | 열교환기 VW 물 온도 | float64 |

#### BTX 증류탑 관련 변수
| 변수명 | 설명 | 데이터 타입 |
|--------|------|-------------|
| `BTXdistillator_RO_flow` | BTX 증류탑 리치오일 유량 | float64 |
| `BTXdistillator_recycle_oil_flow` | BTX 증류탑 리사이클 오일 유량 | float64 |
| `BTXdistillator_top_temp` | BTX 증류탑 상부 온도 | float64 |
| `BTXdistillator_top_pres` | BTX 증류탑 상부 압력 | float64 |
| `BTXdistillator_bottom_pre` | BTX 증류탑 하부 압력 | float64 |

#### 히터 관련 변수
| 변수명 | 설명 | 데이터 타입 |
|--------|------|-------------|
| `Heater_pres` | 히터 압력 | float64 |
| `Heater_temp` | 히터 온도 | float64 |

#### 석탄 배합 관련 변수
| 변수명 | 설명 | 데이터 타입 |
|--------|------|-------------|
| `coal_class` | 석탄 등급 | object (범주형) |
| `batch_no` | 배치 번호 | object (범주형) |
| `호주탄` | 호주탄 배합비 | int64 |
| `러시아탄` | 러시아탄 배합비 | int64 |
| `미국탄` | 미국탄 배합비 | int64 |
| `국내탄` | 국내탄 배합비 | int64 |
| `캐나다탄` | 캐나다탄 배합비 | int64 |

### 2.3 출력 변수 (Targets)

| 변수명 | 설명 | 데이터 타입 | 통계 (평균 ± 표준편차) |
|--------|------|-------------|------------------------|
| `BTX_generation` | BTX 생산량 | float64 | 추후 확인 필요 |
| `Heater_steam_input` | 히터 증기 투입량 | float64 | 추후 확인 필요 |
| `BTXdistillator_distl_steam_input` | BTX 증류탑 증기 투입량 | float64 | 추후 확인 필요 |

---

## 3. 개발 모듈 구조

프로젝트는 **3개의 주요 모듈**로 구성되며, UI도 동일하게 3개 섹션으로 구분됩니다.

```
┌─────────────────────────────────────────────────────────────────┐
│                    다중 타겟 예측 시스템                          │
├─────────────────┬─────────────────┬─────────────────────────────┤
│   Module 1      │   Module 2      │   Module 3                  │
│   전처리        │   모델 학습      │   시각화/UI                  │
│   (Preprocessing)│   (Training)   │   (Visualization)           │
├─────────────────┼─────────────────┼─────────────────────────────┤
│ • 데이터 로드    │ • 모델 선택     │ • 예측 입력 폼               │
│ • 결측치 처리    │ • 하이퍼파라미터│ • 결과 비교 대시보드          │
│ • 인코딩        │ • 학습/검증     │ • 효율 분석 차트             │
│ • 스케일링      │ • PKL 저장      │ • 성능 지표 표시             │
│ • Feature선택   │ • AutoML (옵션) │                             │
└─────────────────┴─────────────────┴─────────────────────────────┘
```

---

### Module 1: 데이터 전처리 (Preprocessing)

#### 1.1 개요

| 항목 | 내용 |
|------|------|
| 목적 | 원시 데이터를 모델 학습에 적합한 형태로 변환 |
| 입력 | `4GTP_integrated_with_coal_Raw.xlsx` |
| 출력 | 전처리된 데이터셋 (train/validation/test split) |

#### 1.2 전처리 파이프라인

```
[원시 데이터]
    ↓
[1] 데이터 로드 및 탐색 (EDA)
    ↓
[2] 결측치/이상치 처리
    ↓
[3] 범주형 변수 인코딩 (coal_class, batch_no)
    ↓
[4] 수치형 변수 스케일링 (StandardScaler/MinMaxScaler)
    ↓
[5] 시계열 특성 추가 (년/월/일/시간, 랙 변수)
    ↓
[6] Feature Selection (상관분석, Feature Importance)
    ↓
[7] 데이터 분할 (시간 순서 유지)
    ↓
[전처리 완료 데이터]
```

#### 1.3 상세 처리 항목

| 단계 | 처리 내용 | 방법 |
|------|----------|------|
| 결측치 | 결측값 확인 및 처리 | 평균 대치 / 선형 보간 / 삭제 |
| 이상치 | IQR 또는 Z-score 기반 탐지 | 클리핑 또는 제거 |
| 인코딩 | `coal_class`, `batch_no` | One-Hot Encoding / Label Encoding |
| 스케일링 | 수치형 변수 정규화 | StandardScaler (권장) |
| 시계열 특성 | Date 컬럼 활용 | year, month, day, hour, day_of_week 추출 |
| 랙 특성 | 이전 시점 값 활용 (선택) | lag_1, lag_2, ... (rolling mean 등) |

#### 1.4 데이터 분할 (시계열 유지)

```
| 학습 (Training)        | 검증 (Validation) | 테스트 (Test) |
|------------------------|-------------------|---------------|
| 2021-02 ~ 2024-06      | 2024-07 ~ 2024-12 | 2025-01 ~     |
| 약 70%                 | 약 15%            | 약 15%        |
```

> ⚠️ **주의**: 시계열 데이터이므로 **랜덤 셔플 금지**. 시간 순서를 유지하여 분할.

#### 1.5 AutoML 옵션

가능하다면 AutoML 도구를 활용하여 전처리 자동화:

| 도구 | 설명 | 장점 |
|------|------|------|
| **PyCaret** | Low-code ML 라이브러리 | 자동 전처리, 모델 비교 |
| **Auto-sklearn** | sklearn 기반 AutoML | 자동 feature engineering |
| **TPOT** | 유전 알고리즘 기반 | 파이프라인 최적화 |
| **H2O AutoML** | 엔터프라이즈급 AutoML | 대용량 데이터 처리 |

#### 1.6 UI 구성 (전처리 섹션)

```
┌────────────────────────────────────────────────────────────┐
│  📊 Module 1: 데이터 전처리                                 │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  [파일 업로드] ┌──────────────────────────────────┐        │
│               │ 4GTP_integrated_with_coal_Raw.xlsx │        │
│               └──────────────────────────────────┘        │
│                                                            │
│  ── 전처리 옵션 ──────────────────────────────────         │
│  ☑ 결측치 처리: [평균 대치 ▼]                              │
│  ☑ 이상치 처리: [IQR 방식 ▼]                               │
│  ☑ 스케일링:   [StandardScaler ▼]                         │
│  ☑ 시계열 특성 추가                                        │
│  ☐ AutoML 자동 전처리                                      │
│                                                            │
│  ── 데이터 분할 ──────────────────────────────────         │
│  Train: [70]% | Validation: [15]% | Test: [15]%            │
│                                                            │
│  [🔄 전처리 실행]  [📥 전처리 데이터 다운로드]              │
│                                                            │
│  ── 결과 미리보기 ────────────────────────────────         │
│  ┌────────────────────────────────────────────────┐       │
│  │  처리 전: 37,039행 x 32열                       │       │
│  │  처리 후: 37,039행 x 45열 (특성 추가됨)          │       │
│  │  결측치: 0개 → 0개                              │       │
│  │  이상치 제거: 127개 행                          │       │
│  └────────────────────────────────────────────────┘       │
└────────────────────────────────────────────────────────────┘
```

---

### Module 2: 모델 학습 (Training)

#### 2.1 개요

| 항목 | 내용 |
|------|------|
| 목적 | 다중 타겟 예측 모델 학습 및 저장 |
| 입력 | 전처리된 데이터셋 |
| 출력 | 학습된 모델 파일 (`.pkl`) |

#### 2.2 다중 타겟 회귀 전략

```
┌─────────────────────────────────────────────────────────┐
│                  다중 타겟 예측 전략                      │
├─────────────────────────┬───────────────────────────────┤
│   방법 1: Native        │   방법 2: Wrapper             │
│   Multi-Output          │   MultiOutputRegressor        │
├─────────────────────────┼───────────────────────────────┤
│ • RandomForest          │ • XGBoost + Wrapper           │
│ • LinearRegression      │ • LightGBM + Wrapper          │
│ • DecisionTree          │ • SVR + Wrapper               │
│ • Neural Network        │ • CatBoost + Wrapper          │
└─────────────────────────┴───────────────────────────────┘
```

#### 2.3 후보 알고리즘

| 알고리즘 | Multi-Output 지원 | 장점 | 단점 |
|----------|-------------------|------|------|
| **RandomForestRegressor** | ✅ Native | 안정적, 과적합 방지 | 속도 느림 |
| **XGBRegressor** | ⚠️ Wrapper 필요 | 높은 성능, 빠른 학습 | 튜닝 필요 |
| **LightGBM** | ⚠️ Wrapper 필요 | 대용량 데이터 효율적 | 과적합 주의 |
| **LinearRegression** | ✅ Native | 해석 용이 | 비선형 패턴 한계 |
| **MLP (Neural Network)** | ✅ Native | 복잡한 패턴 학습 | 많은 데이터 필요 |
| **CatBoost** | ⚠️ Wrapper 필요 | 범주형 자동 처리 | 학습 시간 |

#### 2.4 하이퍼파라미터 튜닝

```python
# 예시: RandomForest 하이퍼파라미터 그리드
param_grid = {
    'n_estimators': [100, 200, 500],
    'max_depth': [10, 20, 30, None],
    'min_samples_split': [2, 5, 10],
    'min_samples_leaf': [1, 2, 4]
}

# 튜닝 방법
- GridSearchCV (전체 탐색)
- RandomizedSearchCV (랜덤 탐색)
- Optuna (베이지안 최적화) ← 권장
```

#### 2.5 학습 프로세스

```
[전처리된 데이터]
    ↓
[1] 모델 후보 선택 (RF, XGB, LightGBM 등)
    ↓
[2] 하이퍼파라미터 튜닝 (Cross-Validation)
    ↓
[3] 모델 학습 (Training Set)
    ↓
[4] 성능 평가 (Validation Set)
    ↓
[5] 최적 모델 선택
    ↓
[6] Test Set 최종 평가
    ↓
[7] 모델 저장 (.pkl)
    ↓
[학습 완료 모델]
```

#### 2.6 모델 저장 형식

```python
import joblib

# 저장 항목
model_artifacts = {
    'model': trained_model,           # 학습된 모델
    'scaler': fitted_scaler,          # 전처리 스케일러
    'encoder': fitted_encoder,        # 범주형 인코더
    'feature_names': feature_list,    # 입력 피처 목록
    'target_names': target_list,      # 타겟 변수 목록
    'metadata': {
        'trained_date': '2025-11-28',
        'version': '1.0',
        'performance': {'R2': 0.85, 'RMSE': 0.12}
    }
}

# 저장
joblib.dump(model_artifacts, 'multi_target_model_v1.0.pkl')
```

#### 2.7 UI 구성 (모델 학습 섹션)

```
┌────────────────────────────────────────────────────────────┐
│  🤖 Module 2: 모델 학습                                     │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  ── 모델 선택 ────────────────────────────────────         │
│  ○ RandomForest    ○ XGBoost    ○ LightGBM                │
│  ○ LinearRegression ○ Neural Network ● AutoML (추천)      │
│                                                            │
│  ── 타겟 변수 ────────────────────────────────────         │
│  ☑ BTX_generation                                          │
│  ☑ Heater_steam_input                                      │
│  ☑ BTXdistillator_distl_steam_input                        │
│                                                            │
│  ── 하이퍼파라미터 ──────────────────────────────          │
│  ┌────────────────────────────────────────────┐           │
│  │ n_estimators: [200]  max_depth: [20]       │           │
│  │ learning_rate: [0.1] min_samples: [5]      │           │
│  └────────────────────────────────────────────┘           │
│  ☑ 자동 튜닝 (Optuna)                                      │
│                                                            │
│  [🚀 학습 시작]                                             │
│                                                            │
│  ── 학습 진행 상황 ──────────────────────────────          │
│  ████████████████░░░░ 80% | Epoch 8/10                     │
│  현재 R²: 0.847 | RMSE: 0.132                              │
│                                                            │
│  ── 학습 완료 시 ─────────────────────────────────         │
│  [📥 모델 다운로드 (.pkl)]  [📊 성능 리포트 보기]           │
└────────────────────────────────────────────────────────────┘
```

---

### Module 3: 시각화 및 UI (Visualization)

#### 3.1 개요

| 항목 | 내용 |
|------|------|
| 목적 | 예측 실행, 결과 비교, 효율 분석 시각화 |
| 입력 | 사용자 입력 또는 실시간 데이터 |
| 출력 | 예측 결과, 비교 차트, 효율 지표 |

#### 3.2 주요 기능

| 기능 | 설명 |
|------|------|
| **예측 실행** | 입력 조건 기반 3개 타겟 동시 예측 |
| **현재값 비교** | 예측값 vs 실제 운영값 비교 표시 |
| **효율 분석** | 개선율 (%) 및 절감량 계산 |
| **추세 시각화** | 시간에 따른 예측/실제값 추이 |
| **성능 대시보드** | 모델 R², RMSE 등 지표 표시 |

#### 3.3 UI 레이아웃

```
┌─────────────────────────────────────────────────────────────────────┐
│  📈 Module 3: 예측 및 시각화                                         │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌─────────────────────────┐  ┌─────────────────────────────────┐  │
│  │  📥 입력 섹션            │  │  📊 예측 결과                    │  │
│  ├─────────────────────────┤  ├─────────────────────────────────┤  │
│  │ BS_out_COG_T: [26.5]    │  │                                 │  │
│  │ BS_out_COG_P: [645.0]   │  │  ┌─────────────────────────────┐│  │
│  │ BS_out_COG_F: [85.2]    │  │  │     항목      │ 현재  │ 예측 ││  │
│  │ Tank_RO_T:    [52.3]    │  │  ├─────────────────────────────┤│  │
│  │ ... (기타 입력값)       │  │  │ BTX 생산량    │ 2.45  │ 2.58 ││  │
│  │                         │  │  │ Heater 증기   │ 5.20  │ 4.85 ││  │
│  │ ── 현재 운영값 ──       │  │  │ BTX증류 증기  │ 3.10  │ 2.95 ││  │
│  │ BTX_현재:     [2.45]    │  │  └─────────────────────────────┘│  │
│  │ Heater_현재:  [5.20]    │  │                                 │  │
│  │ BTX증류_현재: [3.10]    │  │  ── 효율 개선 분석 ──            │  │
│  │                         │  │  ┌─────────────────────────────┐│  │
│  │ [🔮 예측 실행]          │  │  │ BTX 생산: ↑ +5.3% 향상       ││  │
│  └─────────────────────────┘  │  │ Heater:   ↓ -6.7% 절감       ││  │
│                                │  │ BTX증류:  ↓ -4.8% 절감       ││  │
│                                │  └─────────────────────────────┘│  │
│                                └─────────────────────────────────┘  │
│                                                                     │
│  ── 시계열 추이 차트 ───────────────────────────────────────────   │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  [BTX 생산량 추이 그래프]                                    │   │
│  │    ━━━ 실제값   ┅┅┅ 예측값                                  │   │
│  │  ▲                                                          │   │
│  │  │     ╱╲    ╱╲                                             │   │
│  │  │   ╱    ╲╱    ╲                                           │   │
│  │  └──────────────────────────────────────────────────▶ 시간  │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  ── 모델 성능 지표 ─────────────────────────────────────────────   │
│  ┌──────────────┬──────────────┬──────────────┬──────────────┐    │
│  │    타겟      │     R²      │    RMSE     │     MAE      │    │
│  ├──────────────┼──────────────┼──────────────┼──────────────┤    │
│  │ BTX 생산량   │    0.892    │    0.087    │    0.065     │    │
│  │ Heater 증기  │    0.847    │    0.142    │    0.098     │    │
│  │ BTX증류 증기 │    0.865    │    0.118    │    0.082     │    │
│  └──────────────┴──────────────┴──────────────┴──────────────┘    │
└─────────────────────────────────────────────────────────────────────┘
```

#### 3.4 시각화 항목

| 차트 유형 | 용도 | 라이브러리 |
|-----------|------|------------|
| **비교 바 차트** | 현재값 vs 예측값 비교 | Plotly / Altair |
| **시계열 라인 차트** | 시간별 추이 표시 | Plotly |
| **게이지 차트** | 효율 개선율 표시 | Plotly |
| **히트맵** | 변수 간 상관관계 | Seaborn |
| **잔차 플롯** | 모델 오차 분포 | Matplotlib |

---

## 4. 기능 요구사항

### 4.1 필수 기능 (Must Have)

| ID | 기능 | 설명 | 모듈 |
|----|------|------|------|
| F-01 | 데이터 로드 | Excel 파일 업로드 및 파싱 | Module 1 |
| F-02 | 자동 전처리 | 결측치, 이상치, 스케일링 자동 처리 | Module 1 |
| F-03 | 다중 타겟 학습 | 3개 타겟 동시 예측 모델 학습 | Module 2 |
| F-04 | 모델 저장 | PKL 파일로 모델 직렬화 | Module 2 |
| F-05 | 예측 실행 | 입력값 기반 실시간 예측 | Module 3 |
| F-06 | 결과 비교 | 예측값 vs 현재값 비교 표시 | Module 3 |
| F-07 | 효율 계산 | 개선율 (%) 자동 계산 | Module 3 |

### 4.2 권장 기능 (Should Have)

| ID | 기능 | 설명 | 모듈 |
|----|------|------|------|
| F-08 | AutoML 지원 | PyCaret 등 활용 자동 모델 선택 | Module 1, 2 |
| F-09 | 시계열 특성 | 랙 변수, 롤링 통계 자동 생성 | Module 1 |
| F-10 | 성능 리포트 | R², RMSE, MAE 자동 계산 및 표시 | Module 2, 3 |
| F-11 | 추세 시각화 | 시간별 예측/실제값 그래프 | Module 3 |

### 4.3 선택 기능 (Could Have)

| ID | 기능 | 설명 | 모듈 |
|----|------|------|------|
| F-12 | 실시간 데이터 연동 | 공장 DB 자동 연결 | Module 3 |
| F-13 | Feature Importance | 변수 중요도 시각화 | Module 2, 3 |
| F-14 | SHAP 분석 | 예측 설명 기능 | Module 3 |
| F-15 | 알림 기능 | 효율 저하 시 알림 | Module 3 |

---

## 5. 성능 평가 지표

### 5.1 평가 지표 정의

| 지표 | 수식 | 목표값 |
|------|------|--------|
| **R² (결정계수)** | 1 - (SS_res / SS_tot) | ≥ 0.80 |
| **RMSE (평균제곱근오차)** | √(Σ(y-ŷ)²/n) | 최소화 |
| **MAE (평균절대오차)** | Σ\|y-ŷ\|/n | 최소화 |
| **MAPE (평균절대백분율오차)** | Σ\|(y-ŷ)/y\|/n × 100 | ≤ 10% |

### 5.2 타겟별 성능 목표

| 타겟 변수 | R² 목표 | 비고 |
|-----------|---------|------|
| BTX_generation | ≥ 0.85 | 주요 KPI |
| Heater_steam_input | ≥ 0.75 | 에너지 효율 지표 |
| BTXdistillator_distl_steam_input | ≥ 0.75 | 에너지 효율 지표 |

### 5.3 검증 방법

```
┌─────────────────────────────────────────────────────────┐
│                    검증 프로세스                         │
├─────────────────────────────────────────────────────────┤
│  1. 시계열 분할 (Time-based Split)                      │
│     └─ 학습: 2021-2024 | 테스트: 2025                   │
│                                                         │
│  2. 교차 검증 (Time Series Cross-Validation)            │
│     └─ Expanding Window 또는 Sliding Window             │
│                                                         │
│  3. 최종 평가 (Hold-out Test Set)                       │
│     └─ 2025년 데이터로 최종 성능 검증                    │
└─────────────────────────────────────────────────────────┘
```

---

## 6. 배포 및 운영

### 6.1 기술 스택

| 영역 | 기술 |
|------|------|
| **언어** | Python 3.9+ |
| **데이터 처리** | pandas, numpy |
| **머신러닝** | scikit-learn, XGBoost, LightGBM |
| **AutoML** | PyCaret (선택) |
| **UI 프레임워크** | Streamlit (권장) 또는 Gradio |
| **시각화** | Plotly, Altair, Matplotlib |
| **모델 저장** | joblib, pickle |
| **버전 관리** | Git, GitHub |

### 6.2 프로젝트 구조

```
화성공장/
├── SPECKIT_다중타겟예측모델.md    # 본 문서
├── 4GTP_integrated_with_coal_Raw.xlsx  # 원본 데이터
├── src/
│   ├── __init__.py
│   ├── preprocessing.py           # Module 1: 전처리
│   ├── training.py                # Module 2: 모델 학습
│   └── visualization.py           # Module 3: 시각화
├── models/
│   └── multi_target_model_v1.0.pkl  # 학습된 모델
├── app/
│   └── streamlit_app.py           # Streamlit UI
├── notebooks/
│   └── EDA_exploration.ipynb      # 탐색적 분석
├── requirements.txt               # 의존성 목록
└── README.md                      # 프로젝트 설명
```

### 6.3 배포 환경

| 환경 | 설명 |
|------|------|
| **개발** | 로컬 PC (VS Code + Python venv) |
| **스테이징** | 사내 서버 또는 클라우드 VM |
| **운영** | AWS EC2 / Azure VM / Docker Container |

### 6.4 외부 접근성

```
┌─────────────────────────────────────────────────────────┐
│                   배포 아키텍처                          │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  [사용자] ──HTTPS──▶ [클라우드 서버]                    │
│    │                    │                               │
│    │                    ├── Streamlit App (Port 8501)   │
│    │                    ├── 학습된 모델 (.pkl)          │
│    │                    └── 인증/권한 관리               │
│    │                                                    │
│  (VPN 또는 인증 필요)                                    │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 7. 향후 계획

### 7.1 단계별 로드맵

| 단계 | 내용 | 상태 |
|------|------|------|
| **Phase 1** | 데이터 탐색 및 전처리 파이프라인 구축 | 🔜 예정 |
| **Phase 2** | 다중 타겟 모델 학습 및 PKL 저장 | 🔜 예정 |
| **Phase 3** | Streamlit UI 개발 (3개 모듈) | 🔜 예정 |
| **Phase 4** | 테스트 및 성능 검증 | 🔜 예정 |
| **Phase 5** | 클라우드 배포 및 운영 | 🔜 예정 |

### 7.2 확장 계획

- **실시간 데이터 연동**: 공장 DB/OPC 서버 연결
- **모델 재학습 자동화**: 주기적 모델 업데이트 파이프라인
- **처방적 분석**: 최적 운전 조건 추천 기능
- **알림 시스템**: 효율 저하 시 자동 알림
- **다른 공정 확장**: 유사 공정에 모델 적용

---

## 부록

### A. 용어 정의

| 용어 | 정의 |
|------|------|
| BTX | Benzene, Toluene, Xylene의 약자. 방향족 화합물 |
| COG | Coke Oven Gas. 코크스 오븐 가스 |
| RO | Rich Oil. 리치 오일 (흡수유에 BTX가 흡수된 상태) |
| AO | Absorbing Oil. 흡수유 |
| HE | Heat Exchanger. 열교환기 |

### B. 참고 자료

- scikit-learn Multi-output Regression: [링크](https://scikit-learn.org/stable/modules/multiclass.html#multioutput-regression)
- PyCaret Documentation: [링크](https://pycaret.org/)
- Streamlit Documentation: [링크](https://docs.streamlit.io/)

---

> **문서 작성**: Claude Code
> **최종 수정**: 2025-11-28
